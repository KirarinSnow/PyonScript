%!PS

% PyonScript programming language
% Definitions: Input/output operations


% stderr printing operations corresponding to =, ==, and =only
/err=
{
    (%stderr) (w) file exch write=
} bind odef

/err==
{
    (%stderr) (w) file exch write==
} bind odef

/errdisplay
{
    (%stderr) (w) file exch write=only
} bind odef


% get -- read a PostScript token from stdin
%   get -> obj
/get
{
    (%stdin) (r) file token not
    {
	% TODO: Handle I/O error properly
	ioerror
    } if
} bind odef

% getline -- read a PostScript line from stdin
%   getline -> string
/getline
{
    (%stdin) (r) file STRINGMAX string readline not
    {
	% TODO: Handle I/O error properly
	ioerror
    } if
} bind odef

% getstring -- read a PostScript string of a given size from stdin
%   int getstring -> string
/getstring
{
    (%stdin) (r) file exch string readstring not
    {
	% TODO: Handle I/O error properly
	ioerror
    } if
} bind odef

% getchar -- read a character from stdin (returns int)
%   getchar -> int
/getchar
{
    (%stdin) (r) file read not
    {
	% TODO: Handle I/O error properly
	ioerror
    } if
} bind odef
